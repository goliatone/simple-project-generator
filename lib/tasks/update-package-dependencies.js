'use strict';

const DEFAULTS = {
    makePackagePath: function(context) {

    }
};

function updatePakcageDeps(context) {
    //load new project package:
    let pkg =
}

module.exports = updatePakcageDeps;
